{% extends "layout.html.twig" %}

{% block pageTitle %}Контакты представительства в г.{{ representative.geoCityName }} в интернет-магазине Vseinet.ru{% endblock %}

{% block content %}
<div class="breadcrumbs">
    <a href="/contacts">Контакты</a>
</div>

<h1>Торговая точка &laquo;{{ representative.geoPointName }}&raquo;</h1>
<h2>Адрес</h2>
<p>{{ representative.geoCityName }}{% if representative.address %}, {{ representative.address }}{% endif %}</p><br/>
<h2>Телефон</h2>
<p>
    {% if representative.geoPointId == 341 %}Курьер: (8412) 220-212, 8(927)378-1743<br/><br/>{% endif %}
    {% if representative.geoPointId == 341 %}Магазин: {% endif %}{% for phone in representative.phones %}
        {{ phone.value }}{% if not loop.last %},{% endif %}
    {% endfor %}{% if representative.geoPointId == 341 %} (консультация по товару или заказу){% endif %}
</p>
<br/>

<h2>График работы</h2>
<table class="schedule">
    <tr>
        {% for day in ['пн', 'вт', 'ср', 'чт', 'пт', 'сб', 'вс'] %}
            <td>{{ day }}</td>
        {% endfor %}
    </tr>
    <tr>
        {% for schedule in representative.schedule %}
            <td colspan="{{ schedule.count }}">{{ schedule.time }}</td>
        {% endfor %}
    </tr>
</table>
<br/>

<h2>Процесс оформления заказа</h2>
<p>
    1) Вы можете оформить заказ через наш сайт или по указанным телефонам.<br/>
    2) В течение суток с вами свяжется менеджер и сообщит точную информацию по наличию и срокам доставки, конечную стоимость товара, а также необходимо ли вам будет вносить предоплату и способы ее внесения.
</p>

<div class="content">
    <p>
        {% for photo in representative.photos %}
            <span itemscope itemtype="http://schema.org/ImageObject">
            <img src="{{ uploads_domain }}/contacts/{{ photo.url }}" alt="Фото №{{ loop.index }} представительства в г.{{ representative.geoCityName }}" itemprop="contentUrl" />
            </span>
            {% if loop.index % 2 == 1 %}
                &nbsp;
            {% endif %}
            {% if not loop.first and loop.index % 2 == 0 %}
                </p><p>
            {% endif %}
        {% endfor %}
    </p>
</div>
{% endblock %}

{% block stylesheets %}
<style>
    .schedule TD {
        border: 1px solid #ccc;
        padding: 3px 10px;
        text-align: center;
        vertical-align: middle;
    }
    .content {
        margin-top: 15px;
    }
</style>
{% endblock %}
