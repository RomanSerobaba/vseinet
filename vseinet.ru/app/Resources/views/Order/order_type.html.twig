<fieldset class="block">
    <div class="wrapper">
        <h2>Заказ на</h2>
        {% for choice in form.typeCode.vars.choices %}
        <div class="row h">
            <label for="create_form_typeCode_{{ choice.value }}">
                {{ choice.label }}
                <input type="radio" id="create_form_typeCode_{{ choice.value }}" name="create_form[typeCode]" value="{{ choice.value }}" required="required"{% if (choice.value == form.typeCode.vars.value)  %} checked="checked"{% endif %} />
            </label>
        </div>
        {% endfor %}
    </div>
</fieldset>